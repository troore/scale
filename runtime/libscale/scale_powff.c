#include <math.h>
/* PURE _scale_powff PURE */
float _scale_powff(float e1, float e2)
{
  return pow((double) e1, (double) e2);
}

float _scale_powif(int e1, float e2)
{
  return pow((double) e1, (double) e2);
}
